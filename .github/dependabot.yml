version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Wait 1 week after a release before opening a bump PR (avoids same-day
    # bad releases; also gates the ocr CLI, now pinned via the action SHA).
    cooldown:
      default-days: 7
